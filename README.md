# seo-project-2
SEO Project 2

### Initial setup (devs)

```
python3 -m venv .venv
```

```
source .venv/bin/activate
```

To leave the virtual environment, do `deactivate`.

Then, install dependencies

` pip install react flask requests `

